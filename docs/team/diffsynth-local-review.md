# DiffSynth 本地页面检查与待验收事项

日期：2026-09-21。基于 #371 的 a5a2140，包含本地补丁。

## 已完成

- 安装任务在本机完成，环境审计返回 ready：PyTorch 2.8.0+cu128，CUDA 12.8，GPU 可用。这不是实际训练通过。
- 用户查看了参数页，确认保留原有秋叶系的 TOML 代码式预览，不采用左右分列或“易读/TOML”双视图。
- 仅对 Sample 的显示做分组、分行，长文本换行；复制、导出和提交仍使用原始 JSON 字符串数组格式。显示中的数组表仅用于阅读。
- processor_path 放到表单末尾的“可选模型配置”，一般留空自动查找，失败时再指定。
- Qwen 默认提供一个 Sample，使用用户指定的和服人物提示词；不覆盖已有配置，新增 Sample 仍可自行填写。
- 防护：明确拒绝编辑任务及非空参考目录；非法数值与 Sample 返回字段错误，不创建任务。
- 本次复跑：14 项前端针对性测试、23 项后端防护测试通过。此前类型检查和构建通过；不等同于全量回归。

## 尚未验收

用户当前没有可用于实训的完整模型，本轮暂停实际训练验收。后台发起过权重下载，但不能据此认定文件完整或模型加载成功。

后续具备模型后需要检查：

1. Comfy-Org 三个 BF16 组件与 Processor 实际加载；完整目录及分别指定组件两种入口。
2. 图片 + TXT、混合重复次数目录和原生元数据；确认样本与优化器更新次数计算。
3. 每 N 次更新生成多个 Sample，分别使用各自参数；出图后继续训练。
4. 训练、采样期间停止任务，确认子进程退出且显存释放。
5. 配置导入导出、任务回填、切换输入方式不串参数。
6. 实际训练生成的 LoRA 在原生 ComfyUI 节点回载出图。

CPU 模型卸载与训练预览仍不能同时开启；编辑训练、多卡和量化不在本次首发范围。
本记录不是合并批准，不应将环境 ready 或页面检查写成实机训练验收通过。
